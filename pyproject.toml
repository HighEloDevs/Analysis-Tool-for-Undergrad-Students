[build-system]
requires = [
    "setuptools",
    "wheel",
    "matplotlib==3.4.2",
    "pandas",
    "numpy",
    "scipy",
    "lmfit",
    "pyqt5",
    "requests",
]
build-backend = "setuptools.build_meta"