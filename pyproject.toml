[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "matplotlib==3.4.2",
    "pandas",
    "numpy",
    "scipy",
    "lmfit",
    "matplotlib_backend_qtquick",
    "pyqt5",
    "requests",
]
build-backend = "setuptools.build_meta"